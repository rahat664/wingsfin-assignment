# WingsFin Frontend

This package contains the React client for the WingsFin real-time market chart assignment.

For setup, run instructions, and the architecture overview, see the repository root [README](../README.md).

Local development:

```bash
npm install
npm run dev
```

Production build:

```bash
npm run build
```

